<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/phrameRecycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/miniDetailsLayout"/>

    <FrameLayout
        android:id="@+id/miniDetailsLayout"
        android:clipChildren="false"
        android:layout_width="match_parent"
        app:layout_constraintBottom_toTopOf="@id/miniDetailsButton"
        android:layout_height="250dp"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Mini Details View"
        android:background="@android:color/transparent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:id="@+id/miniDetailsButton"/>

</androidx.constraintlayout.widget.ConstraintLayout>